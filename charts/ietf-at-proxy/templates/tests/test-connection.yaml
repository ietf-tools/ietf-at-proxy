apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ietf-at-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "ietf-at-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ietf-at-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
