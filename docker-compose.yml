---
version: '3.3'
services:
  api:
    build: ietf-at/.
    container_name: api
    restart: "unless-stopped"
  ui:
    build: ietf-at-ui/.
    container_name: ui
    restart: "unless-stopped"
  proxy:
    build: .
    depends_on:
      - api
      - ui
    links:
      - api
      - ui
    ports:
      - '80:80'
      - '443:443'
    restart: "unless-stopped"
